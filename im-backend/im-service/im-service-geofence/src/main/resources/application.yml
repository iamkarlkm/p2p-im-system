server:
  port: 8083

spring:
  application:
    name: im-service-geofence
  datasource:
    url: jdbc:mysql://localhost:3306/im_geofence?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: karl1972
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true

# Redis配置 (可选，用于GeoHash索引优化)
spring.data.redis:
  host: localhost
  port: 6379
  database: 1

# 日志配置
logging:
  level:
    com.im.service.geofence: INFO
    org.springframework: WARN
